<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Nexy</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
<nav class="navbar">
    <a href="/" class="navbar__brand">Nexy</a>
    <div class="navbar__links">
        <a href="/Feed" class="navbar__link">Фид</a>
        <a href="/Profile" class="navbar__link">Профиль</a>
        <a href="/Admin" class="navbar__link">Админка</a>
        @if (User.Identity.IsAuthenticated)
        {
            <a href="/Identity/Account/Logout" class="navbar__link">Выйти</a>
        }
        else
        {
            <a href="/Identity/Account/Login" class="navbar__link">Войти</a>
        }
    </div>
</nav>

<main>
    @RenderBody()
</main>

@RenderSection("Scripts", required: false)
</body>
</html>